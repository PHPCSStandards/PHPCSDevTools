<?xml version="1.0"?>
<documentation title="The title of this documentation is too long and should throw a validation error even though the code is correct.">
    <standard>
    <![CDATA[
      This is an example of the correct documentation example.
    ]]>
    </standard>
    <code_comparison>
        <code title="Valid: A valid code example">
        <![CDATA[
$number = 3;
if(3 <em>===</em> $number) {
    echo 'Number equals three.';
}
        ]]>
        </code>
        <code title="Invalid: An invalid code example">
        <![CDATA[
$number = 3;
if(3 <em>=</em> $number) {
    echo 'Number equals three.';
}
        ]]>
        </code>
    </code_comparison>
</documentation>
